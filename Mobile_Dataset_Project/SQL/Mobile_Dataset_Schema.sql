create database Mobile;
use Mobile;
create table Mobile_data(
id int primary key,
model varchar (255),
price int,
rating float ,
os varchar (255),
sim_type varchar (255),
network_type varchar (255),
VoLTE varchar (255),
NFC varchar (255),
ir_blaster varchar (255),
chipset varchar (255),
core_type varchar (255),
clock_ghz float,
ram_gb float,
storage_gb float,
battery_mah float,
fast_charge_w float,
screen_size_in float,
resolution varchar (225),
refresh_rate_hz float,
display_type varchar (100),
rear_camera_mp_list varchar(225),
rear_camera_count int ,
rear_camera_max_mp float,
front_camera_mp float,
memory_card_supported int,
memory_card_type varchar(225),
memory_card_max_gb float
);
